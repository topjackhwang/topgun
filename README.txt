* Prequisite
    1. python installed
    2. web.py installed, if it's not, it can be installed as below:
        sudo easy_install web.py

* Deploy and run
    1. run ./deploy.sh
    2. run ./fibonacci.py to start the web service

* Test
    1. run ./tests/testcase.py
